version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: kp-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - app-network

volumes:
  redis-data:

networks:
  app-network:
    external: true
