# Database Configuration
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=root
DB_NAME=sms_2_0

# Redis Configuration
REDIS_URL=redis://redis:6379

# Server Configuration
PORT=4000
JWT_SECRET=supersecretkey

# Application Configuration
# IMPORTANT: This must be the URL where the backend is accessible from the user's browser.
# For local development: http://localhost:4000
# For GCP VM (Public IP): http://YOUR_VM_PUBLIC_IP:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
